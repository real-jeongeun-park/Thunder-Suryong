drop table if exists quiz_folder cascade;

create table quiz_folder (
    id bigint generated by default as identity,
    nickname varchar(30),
    exam_id varchar(36),
    folder_id varchar(36),
    folder_name varchar(50),
    primary key (id)
);